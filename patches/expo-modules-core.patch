diff --git a/ios/Legacy/Services/Permissions/EXPermissionsService.m b/ios/Legacy/Services/Permissions/EXPermissionsService.m
index 00dbd944b81fdbf1595930e8194b6794e51203f0..ef8c0c0cb59301124b6de901a3fe5cb80795e9c4 100644
--- a/ios/Legacy/Services/Permissions/EXPermissionsService.m
+++ b/ios/Legacy/Services/Permissions/EXPermissionsService.m
@@ -31,9 +31,11 @@ NSString * const EXPermissionExpiresNever = @"never";
 }

 - (void)registerRequesters:(NSArray<id<EXPermissionsRequester>> *)newRequesters {
-  for (id<EXPermissionsRequester> requester in newRequesters) {
-    [_requesters setObject:requester forKey:[[requester class] permissionType]];
-    [_requestersByClass setObject:requester forKey:[requester class]];
+  @synchronized (self) {
+    for (id<EXPermissionsRequester> requester in newRequesters) {
+      [_requesters setObject:requester forKey:[[requester class] permissionType]];
+      [_requestersByClass setObject:requester forKey:[requester class]];
+    }
   }
 }

@@ -162,12 +164,16 @@ NSString * const EXPermissionExpiresNever = @"never";

 - (id<EXPermissionsRequester>)getPermissionRequesterForType:(NSString *)type
 {
-  return _requesters[type];
+  @synchronized (self) {
+    return _requesters[type];
+  }
 }

 - (id<EXPermissionsRequester>)getPermissionRequesterForClass:(Class)requesterClass
 {
-  return [_requestersByClass objectForKey:requesterClass];
+  @synchronized (self) {
+    return [_requestersByClass objectForKey:requesterClass];
+  }
 }

 @end
