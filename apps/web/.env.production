# Public build-time configuration for the production site.
# These are intentionally committed: both values are public (the Supabase URL
# ships in the mobile app bundle; the email is on the site). Vercel picks
# them up at build time, so no dashboard env configuration is required.
NEXT_PUBLIC_SUPABASE_URL=https://baxgullapuksjbzkogii.supabase.co
NEXT_PUBLIC_OLW_CONTACT_EMAIL=support@ourlittleworld.me
